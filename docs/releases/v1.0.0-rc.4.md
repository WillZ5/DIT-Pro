# v1.0.0-rc.4 — 修复与品牌更新 / Fixes and Branding Refresh

## 问题修复 | Bug Fixes

- **[P1] 测试 schema 对齐**：`abnormal_scenarios` 测试中的 `setup_db()` 现已包含所有生产列（`hash_md5`、`hash_xxh128`、`hash_xxh3`、`retry_note`）；161 项测试全部通过，修复了此前 4 个失败 / **[P1] Test schema alignment**: `setup_db()` in `abnormal_scenarios` now includes all production columns (`hash_md5`, `hash_xxh128`, `hash_xxh3`, `retry_note`); all 161 tests are passing, fixing the previous 4 failures
- **[P2] 恢复状态一致性**：`execute_resume()` 的成功条件现已包含 `overall_failed == 0 && overall_pending == 0`，确保数据库状态、前端事件和返回值保持一致 / **[P2] Resume state consistency**: the `execute_resume()` success condition now includes `overall_failed == 0 && overall_pending == 0`, keeping DB state, frontend events, and return values consistent

## 改进 | Improvements

- **新 Logo**：官网导航中的占位 “D” 图标已替换为正式版 DIT Pro SVG Logo / **New logo**: the placeholder "D" icon in website navigation has been replaced with the official DIT Pro SVG logo
- **官网版本同步**：网站各区块已统一更新到 rc.4，并同步中英文文案 / **Website**: all website sections are updated to rc.4 with both English and Chinese copy
- **品牌色调整**：官网主题强调色从绿色 `#3ecf8e` 调整为品牌蓝 `#1e5bff` / **Accent color**: the website theme accent changed from green `#3ecf8e` to brand blue `#1e5bff`

## 验证 | Verification

| 检查 Check | 状态 Status |
|-------|--------|
| `cargo test` (lib) | 119/119 passed |
| `cargo test` (`abnormal_scenarios`) | 10/10 passed |
| `cargo test`（所有集成测试 / all integration tests） | 161/161 passed |
| `cargo clippy` | clean |
| `cargo fmt --check` | clean |
| `npm run lint` | clean |
| `npm run build` | clean |

## 下载 | Download

- **`DIT.Pro_1.0.0-rc.4_universal.dmg`**（13 MB）- macOS 通用版（Intel + Apple Silicon） / **`DIT.Pro_1.0.0-rc.4_universal.dmg`** (13 MB) - macOS Universal (Intel + Apple Silicon)
